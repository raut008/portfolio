import Timeline from "../Timeline";
import styles from "./index.module.scss";

const Experience = () => {
  return (
    <section id="experience" className={styles["about-me"]}>
      <Timeline />
    </section>
  );
};

export default Experience;
